using UnityEngine;
using System.Collections;

public class Types : MonoBehaviour {

}

public enum PlayerPosition {
	quarterBack, lineBacker, runningBack, stop
}

public enum GameMode {
	play, run, pass, def, move, reset
}